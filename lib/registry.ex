defmodule Matcha.Registry do

  # def count_match(registry, key, pattern, guards \\ []) do

  # def match(registry, key, pattern, guards \\ []) do

  # def select(registry, spec) do

  # def unregister_match(registry, key, pattern, guards \\ []) do

end
